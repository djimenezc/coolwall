extends auth

block auth
  
    form.signup.form-horizontal(action="/users", method="post")
      .control-group
        label.control-label(for='name') Full name
        
        input.form-control#name(type='text', name="name", placeholder='Full name', value=user.name, autofocus='autofocus')

      .control-group
        label.control-label(for='email') Email
        .controls
          input.form-control#email(type='text', name="email", placeholder='Email', value=user.email)

      .control-group
        label.control-label(for='username') Username
        .controls
          input.form-control#username(type='text', name="username", placeholder='Username', value=user.username)

      .control-group
        label.control-label(for='password') Password
        .controls
          input.form-control#password(type='password', name="password", placeholder='Password')

      .form-actions(style='margin-top:1em')
        button.btn.btn-lg.btn-primary.btn-block(type='submit') Sign up
      .control-group
        a.show-login(href="/signin") Sign in
